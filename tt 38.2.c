///Advanced loop. 2
///ctrl z
///add two number in infinity

#include<stdio.h>
int main()
{
    int a,b,sum;
    while(scanf("%d%d",&a,&b)!=EOF)
    {
      //  if(a==0 && b==0)
       // return 0;
        sum=a+b;
        printf("%d\n",sum);
    }
  return 0;
}

